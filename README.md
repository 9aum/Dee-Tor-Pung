# Dee-Tor-Pung (ดีต่อพุง) 🏃‍♂️🥗

**ดีต่อพุง** เป็นแอปพลิเคชันสำหรับบันทึกผลการดูแลสุขภาพและการออกกำลังกาย เพื่อช่วยให้คุณติดตามความก้าวหน้าและไปถึงเป้าหมายได้ง่ายขึ้น

## ✨ ฟีเจอร์หลัก (Features)

*   **📊 Dashboard & Stats:** ดูภาพรวมน้ำหนัก, ระยะทางสะสม, และเวลาที่ใช้ไป พร้อมกราฟแสดงแนวโน้มน้ำหนัก
*   **📝 Daily Log:** บันทึกข้อมูลรายวัน (น้ำหนัก, ระยะทางวิ่ง, เวลาที่ใช้) พร้อมแนบรูปประกอบได้ถึง 3 รูป
*   **📅 History List:** ดูรายการย้อนหลัง แก้ไข หรือลบข้อมูลที่ไม่ต้องการได้
*   **👤 Profile & Goals:** ตั้งค่าเป้าหมายสุขภาพ, คำคมเตือนใจ (Quotes), และรูปโปรไฟล์
*   **🌐 Localization:** รองรับ 2 ภาษา (ไทย และ อังกฤษ)
*   **💾 Offline First:** ข้อมูลทั้งหมดเก็บในเครื่อง (SQLite) ไม่ต้องใช้อินเทอร์เน็ต

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

*   **Framework:** React Native With Expo
*   **Language:** JavaScript
*   **Database:** Expo SQLite (Local Database)
*   **Navigation:** React Navigation
*   **Charts:** React Native Chart Kit
*   **UI Components:** Custom Components & Vector Icons

## 🚀 การติดตั้งและรันโปรเจค (Installation)

1.  **Clone Connect:**
    ```bash
    git clone https://github.com/9aum/Dee-Tor-Pung.git
    cd Dee-Tor-Pung
    ```

2.  **Install Dependencies:**
    ```bash
    npm install
    ```

3.  **Run App:**
    ```bash
    npx expo start
    ```
    *   กด `a` เพื่อเปิดใน Android Emulator
    *   กด `i` เพื่อเปิดใน iOS Simulator
    *   หรือสแกน QR Code ด้วยแอป Expo Go

## 📦 การ Build APK (สำหรับ Android)

โปรเจคนี้ตั้งค่าไว้สำหรับ Build ผ่าน **EAS Build** (Expo Application Services)

1.  **Login Expo:**
    ```bash
    eas login
    ```

2.  **Build Command:**
    ```bash
    eas build -p android --profile preview
    ```
    *รอดำเนินการและดาวน์โหลดไฟล์ .apk จากลิงก์ที่ได้รับ*

## 📱 ผู้พัฒนา
พัฒนาโดย **9aum**
